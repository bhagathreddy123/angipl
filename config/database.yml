default: &default
  adapter: postgresql
  encoding: unicode
  host: localhost
  pool: 5
  username: postgres 
  password: password
  template: template0

development:
  <<: *default
  database: angis

test:
  <<: *default
  database: angis_test
